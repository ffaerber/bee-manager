{
  "name": "swarm-stamp-monitor",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit && cd web && tsc --noEmit",
    "build:web": "cd web && bun run build"
  },
  "dependencies": {
    "elysia": "^1.2.25",
    "@elysiajs/static": "^1.2.0",
    "ethers": "^6.13.5"
  },
  "devDependencies": {
    "@ethersphere/bee-js": "^11.1.1",
    "bun-types": "^1.2.0",
    "typescript": "^5.7.2"
  }
}
